import { SurveyInputDirective } from './survey-input.directive';

describe('SurveyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SurveyInputDirective();
    expect(directive).toBeTruthy();
  });
});
